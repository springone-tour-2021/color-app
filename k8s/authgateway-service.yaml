apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: authgateway
  name: authgateway
spec:
  ports:
  - name: 80-8080
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: authgateway
  type: ClusterIP
status:
  loadBalancer: {}
